# ============================================
# SECRETS.  cp example.secrets.env .secrets.env  → fill in.
# The runtime form (.secrets.env) is gitignored. Loaded LAST in the env-chain
# so these values win over .env and the per-environment overlay.
# NEVER commit real values.
# ============================================

DATABASE_PASSWORD=CHANGE_ME
REDIS_PASSWORD=CHANGE_ME
APP_SECRET=CHANGE_ME
ADMIN_PASSWORD=CHANGE_ME
