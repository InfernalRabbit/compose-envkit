# example.ci.env — CI chain template (C4 named-chain fixture).
# Seeded to .ci.env by `cenvkit init` / stageMonorepo.
# Loaded only when --chain ci is used (see .cenvkit.envchain [ci] section).
CI=true
IS_DEV=false
