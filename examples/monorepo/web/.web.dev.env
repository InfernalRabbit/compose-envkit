# web DEV tier — loaded after .web.env when COMPOSE_ENV=dev (discovered via the
# ${COMPOSE_ENV} token in web/docker-compose.yml's env_file:). Last-wins over
# .web.env. Demo: a dev-only flag. WEB_PORT stays in .web.env (env-agnostic).
WEB_DEBUG=true
