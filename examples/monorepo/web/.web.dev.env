# web DEV tier — loaded after .web.env when COMPOSE_ENV=dev (selected by the
# ${COMPOSE_ENV:-dev} token in web/docker-compose.yml's env_file: path). Last-wins
# over .web.env (both runtime-only). Demo: a dev-only flag. WEB_PORT stays in .web.env.
WEB_DEBUG=true
