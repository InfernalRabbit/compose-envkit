# web DEV tier — loaded after .web.env when CENVKIT_ENV=dev (selected by the
# ${CENVKIT_ENV:-dev} token in web/docker-compose.yml's env_file: path). Last-wins
# over .web.env (both runtime-only). Demo: a dev-only flag. WEB_PORT stays in .web.env.
WEB_DEBUG=true
