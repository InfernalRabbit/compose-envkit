# Service env_file: for the reports subproject (nested under services/).
# REPORTS_PORT is defined ONLY here; cross-subproject Layer-2 folds it in from
# the root (find-depth 3) so ${REPORTS_PORT} resolves in the unified config too.
REPORTS_PORT=15151
