# Service env_file: for the api subproject.
#
# Referenced by api/docker-compose.yml's `env_file:` — configures the container
# at runtime AND (via the kit's Layer-2 discovery) feeds compose-time ${API_PORT}
# interpolation. ONE place, two roles.
#
# In a real project this would be gitignored and seeded from an example.* —
# it carries no secrets here, so the blueprint ships it directly to stay
# runnable out of the box.
API_PORT=19090
