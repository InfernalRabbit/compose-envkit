# example.api-chain.env — API chain template (C4 named-chain fixture).
# Seeded to .api-chain.env by `cenvkit init` / stageMonorepo.
# Loaded only when --chain api is used (see .cenvkit.envchain [api] section).
API_CHAIN=true
STACK_TIER=api
